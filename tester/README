u can run tests in different ways:

KEYS :

bool withoutTokens - not to print Tokens of the Parser

1) run_all(bool withoutTokens)

2) run_one(string name, bool withoutTokens) // run_once("test0")

3) run_tests(int *a, bool withoutTokens),  where a - array of test's id
    // int a[2] = {3, 4};
    // runt_tests(a, 2);

the result of parsing is saved in "/tester/log.txt" and overwritten each time
