int write(int fd, char * buf, int count) {
asm {
mov rax, 4
xor rbx, rbx
xor rdx, rdx
mov ebx, dword[rsb + 8]
mov rcx, qword[rsb + 12]
mov edx, dword[rsb + 20]
int 0x80
}
return;
}

int main() {
	int a;
	int b;
	a = a % b ^ b == a != b != !!!a == "dotadotadotadotadota";
	if (b) return 123;
	if (a) return 0; else return 1;

	char* str[123] = "artur noob";

	c[0] = '0';

	for (int i = 0; i != 123123; i = i + 1) {
		2 + 2;
	}

	for (int i = 0; i != 123123; i = i + 1) 1 + 1;

}
