int write(int fd, char* buf, int count) {
    asm {
        mov rax, 1
        mov rdi, qword[rbp + 8]
        mov rsi, qword[rbp + 16]
        mov rdx, qword[rbp + 24]
        syscall
    }
    return;
}

int main() {
    write(1, "Hello!", 8);
    return 0;
}

